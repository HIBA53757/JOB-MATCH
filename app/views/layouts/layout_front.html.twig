<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Job Dating YouCode{% endblock %}</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-br from-gray-50 to-gray-100 text-gray-800 flex flex-col min-h-screen">

    <nav class="bg-gradient-to-r from-purple-700 via-blue-600 to-indigo-700 shadow-lg">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/user/home" class="text-2xl font-extrabold text-white tracking-tight">
                YouCode<span class="text-blue-200">Jobs</span>
            </a>
            
            <a href="/user/MyDemandes" class="text-white text-2xl font-extrabold tracking-tight">Mon<span class="text-blue-200">Demandes</span></a>

            <div class="flex items-center gap-4">
                <span class="text-sm font-semibold text-white/90">
                    {{ user_name }}
                </span>
                <a
                    href="/logout"
                    class="text-sm bg-white/10 backdrop-blur px-4 py-1.5 rounded-lg text-white hover:bg-white/20 transition"
                >
                    Déconnexion
                </a>
            </div>
        </div>
    </nav>

    <main class="flex-grow container mx-auto px-6 py-10">
        {% block content %}{% endblock %}
    </main>

    <footer class="bg-blue-700 text-gray-300 py-8 mt-auto">
        <div class="container mx-auto text-center text-sm">
            <p>
                © 2024 <span class="font-semibold text-white">YouCode Job Dating</span>.
                Tous droits réservés.
            </p>
        </div>
    </footer>

    {% block scripts %}{% endblock %}
</body>
</html>
